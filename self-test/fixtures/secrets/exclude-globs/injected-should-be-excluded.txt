# Orbit self-test fixture — DELIBERATE fake credential for live QA proof of
# secret-detection.yml's `exclude_globs` input (CR-A079-14 Part O / F4).
# This is the SAME well-known AWS documentation EXAMPLE key used by
# ../injected.txt (never a real credential) — the two fixtures together
# prove exclude_globs both directions with one input pattern:
#   - exclude_globs = "self-test/fixtures/secrets/exclude-globs/*" matches
#     THIS file (path lives under exclude-globs/) -> its secret must be
#     SKIPPED even against an empty baseline (secret-detection-exclude-globs-pass,
#     self-test.yml).
#   - That exact same pattern does NOT match ../injected.txt (a sibling,
#     not a descendant of exclude-globs/) -> its secret must still BLOCK
#     against an empty baseline
#     (secret-detection-exclude-globs-still-blocks-fail, self-test-red-path.yml).
aws_access_key_id = AKIAIOSFODNN7EXAMPLE
aws_secret_access_key = wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY
